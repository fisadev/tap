[project]
name = "ejemplo-docker-actual"
version = "0.1.0"
description = "Ejemplo mínimo para correr con Docker"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.1.2",
    "hrid>=0.3.0",
    "requests>=2.32.5",
]
