[project]
name = "ejemplo-paas"
version = "0.1.0"
description = "Ejemplo mínimo para deployar en un PaaS"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.1.2",
    "psycopg[binary]>=3.2",
]
